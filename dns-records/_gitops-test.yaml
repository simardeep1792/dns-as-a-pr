apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: gitops-test-simardeep-xyz
  namespace: dns
  labels:
    simardeep.xyz/owner: "platform"
spec:
  endpoints:
    - dnsName: _gitops-test.simardeep.xyz
      recordType: TXT
      ttl: 300
      targets:
        - "dns-as-pr-works"
