apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: gitops-test-simardeep-xyz
  namespace: dns
  labels:
    simardeep.xyz/owner: "platform"
spec:
  endpoints:
    - dnsName: gitops-test-a.simardeep.xyz
      recordType: A
      recordTTL: 300
      targets:
        - 203.0.113.10
