apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dns-records
  namespace: argocd
  annotations:
    # Apply DNSEndpoint resources after ExternalDNS installs the CRD.
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://github.com/simardeep1792/dns-as-a-pr.git
    targetRevision: main
    path: dns-records
  destination:
    server: https://kubernetes.default.svc
    namespace: dns
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
